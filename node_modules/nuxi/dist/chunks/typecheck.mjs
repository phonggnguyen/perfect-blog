import { e as execa } from '../shared/nuxi.bc8d057a.mjs';
import { b as tryResolveModule } from '../shared/nuxi.903a28f0.mjs';
import { l as loadKit } from '../shared/nuxi.4e484f51.mjs';
import { w as writeTypes } from '../shared/nuxi.9175a8b6.mjs';
import { d as defineNuxtCommand } from '../shared/nuxi.a865ab6b.mjs';
import { r as resolve } from '../shared/nuxi.6feb7084.mjs';
import 'node:buffer';
import 'node:path';
import 'node:child_process';
import 'node:process';
import '../shared/nuxi.79d40427.mjs';
import 'child_process';
import 'path';
import '../shared/nuxi.7d1333e8.mjs';
import 'fs';
import 'assert';
import 'events';
import 'buffer';
import 'stream';
import 'util';
import 'node:url';
import 'os';
import 'node:os';
import 'node:module';
import 'node:fs';
import '../shared/nuxi.c0e643cf.mjs';

const typecheck = defineNuxtCommand({
  meta: {
    name: "typecheck",
    usage: "npx nuxi typecheck [rootDir]",
    description: "Runs `vue-tsc` to check types throughout your app."
  },
  async invoke(args) {
    process.env.NODE_ENV = process.env.NODE_ENV || "production";
    const rootDir = resolve(args._[0] || ".");
    const { loadNuxt, buildNuxt } = await loadKit(rootDir);
    const nuxt = await loadNuxt({ rootDir, config: { _prepare: true } });
    await writeTypes(nuxt);
    await buildNuxt(nuxt);
    await nuxt.close();
    const hasLocalInstall = tryResolveModule("typescript", rootDir) && tryResolveModule("vue-tsc/package.json", rootDir);
    if (hasLocalInstall) {
      await execa("vue-tsc", ["--noEmit"], { preferLocal: true, stdio: "inherit", cwd: rootDir });
    } else {
      await execa("npx", "-p vue-tsc -p typescript vue-tsc --noEmit".split(" "), { stdio: "inherit", cwd: rootDir });
    }
  }
});

export { typecheck as default };
